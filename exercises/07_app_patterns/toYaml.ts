// TODO: convert an object to a simple YAML string.
// - Skip null/undefined values
// - Nested objects are indented with two spaces
// - Arrays use "-" entries
// - Strings should be unquoted
export function toYaml(obj: object, indent = 0): string {
  return '';
}
